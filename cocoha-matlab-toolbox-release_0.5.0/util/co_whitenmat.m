function X = co_whitenmat(cfg,data)
% CO_WHITENMAT applies a data whitening matrix to a data transformation matrix. This avoids having
% to whiten the data before applying the data transformation matrix.